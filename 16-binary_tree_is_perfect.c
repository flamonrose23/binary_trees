#include "binary_trees.h"

/**
 * binary_tree_is_perfect - Writting a function that checks if a binary tree is perfect
 *@tree: pointer to the root node of the tree to check
 *
 * return: If tree is NULL, a function must return 0
 * 
 */
int binary_tree_is_perfect(const binary_tree_t *tree);
{
